# User Notification Microservices with Kafka

This project demonstrates an event-driven microservices architecture using Kafka.

## Features
- User registration service (`user-service`)
- Notification service using SMS (`notification-service`)
- Kafka as event broker
- Docker Compose to run Kafka locally

## How to run
1. Start Kafka:
```bash
docker-compose up -d
